<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="info.ivicel.tumoblog.admin.mapper.TagMapper">
    <resultMap id="BaseResultMap" type="info.ivicel.tumoblog.admin.entity.Tag">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, `name`
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from tags
        where id = #{id,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from tags
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="info.ivicel.tumoblog.admin.entity.Tag">
        insert into tags (id, `name`)
        values (#{id,jdbcType=BIGINT}, #{name,jdbcType=VARCHAR})
    </insert>

    <insert id="insertSelective" parameterType="info.ivicel.tumoblog.admin.entity.Tag">
        insert into tags
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="name != null">
                `name`,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=BIGINT},
            </if>
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <insert id="insertOnIgnore" parameterType="list">
        insert ignore into `tags` (`name`) values
        <foreach collection="tags" item="tag" separator=",">
            (#{tag.name})
        </foreach>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="info.ivicel.tumoblog.admin.entity.Tag">
        update tags
        <set>
            <if test="name != null">
                `name` = #{name,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="info.ivicel.tumoblog.admin.entity.Tag">
        update tags
        set `name` = #{name,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="count" resultType="java.lang.Long">
        select count(1) from tags;
    </select>

    <select id="findByNameIn" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from `tags` where `name` in
        <foreach collection="tags" item="tag" separator="," open="(" close=")">
            #{tag.name}
        </foreach>
    </select>
</mapper>